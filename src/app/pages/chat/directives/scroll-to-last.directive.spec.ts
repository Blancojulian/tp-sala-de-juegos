import { ScrollToLastDirective } from './scroll-to-last.directive';

describe('ScrollToLastDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToLastDirective();
    expect(directive).toBeTruthy();
  });
});
