import { AhorcadoDirective } from './ahorcado.directive';

describe('AhorcadoDirective', () => {
  it('should create an instance', () => {
    const directive = new AhorcadoDirective();
    expect(directive).toBeTruthy();
  });
});
